namespace Microting.KanbanBase.Infrastructure.Enums;
// Values are persisted — append new members, never renumber existing ones.
public enum UserbackImportLogEntryStatus { Imported = 0, Skipped = 1, Failed = 2, Updated = 3, Deleted = 4 }
